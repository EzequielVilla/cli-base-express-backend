{
  "name": "test-project",
  "version": "1.0.15",
  "description": "",
  "type": "module",
  "bin": "./build/index.mjs",
  "main": "index.mts",
  "scripts": {
    "start": "node ./build/index.mjs",
    "build": "tsc",
    "test": "npm run build && node ./build/index.mjs new test-project",
    "npm:publish": "npm run build && npm version patch && npm publish && git add package.json && git commit -am 'publish' && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^10.0.4"
  }
}