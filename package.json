{
  "name": "cli-base-express-backend",
  "version": "1.0.14",
  "description": "",
  "type": "module",
  "bin": "./build/index.mjs",
  "main": "index.mts",
  "scripts": {
    "start": "node ./build/index.mjs",
    "build": "tsc",
    "npm:publish": "npm run build && npm version patch && npm publish && git add package.json && git commit -m 'publish' && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^10.0.4"
  }
}
